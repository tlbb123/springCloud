server:
  port: 3344

spring:
  application:
    name: cloud-config-center
  cloud:
    config:
      server:
        git:
          uri: https://github.com/tlbb123 #git仓库的名字
          #搜索的目录
          search-paths: springcloud-config
      #读取分支
      label: master

eureka:
  client:
    server-url:
      defaultZone: http://localhost:7002/eureka
